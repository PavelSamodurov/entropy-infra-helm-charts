cluster:
  allowSchedulingOnControlPlanes: true
  apiServer:
    certSANs:
      - 84.22.133.116

machine:
  registries:
    mirrors:
      ghcr.io:
        endpoints:
          - https://ghcr.io
    config:
      ghcr.io:
        auth:
          username: GITHUB_USERNAME
          password: GITHUB_PAT
